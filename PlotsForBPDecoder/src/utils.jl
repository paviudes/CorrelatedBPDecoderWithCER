# This file used to define `fmt_probs`. It's now in
#     CorrelatedBPDecoderWithCER/src/utils.jl
# and re-exported from the main package, so PlotsForBPDecoder pulls it in
# via `using CorrelatedBPDecoderWithCER: fmt_probs` in PlotsForBPDecoder.jl.
#
# You can safely delete this file — the module no longer `include`s it.
